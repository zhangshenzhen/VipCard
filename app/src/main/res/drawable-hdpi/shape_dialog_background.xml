<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充颜色 -->
    <solid android:color="@color/dialog_btn_background_color"></solid>

    <!-- 线的宽度 -->
    <stroke android:width="0.5dp" android:color="@color/dialog_btn_background_color"></stroke>

    <!-- 矩形的圆角半径 -->
    <corners android:radius="6dp" />

</shape>